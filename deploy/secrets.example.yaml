apiVersion: v1
kind: Secret
metadata:
  name: telegram-persona-bot-secrets
type: Opaque
stringData:
  TELEGRAM_BOT_TOKEN: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  TAVILY_API_KEY: "REPLACE_ME"
  BRAVE_API_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql+psycopg://bot:REPLACE_ME@postgres:5432/bot"
  POSTGRES_USER: "bot"
  POSTGRES_PASSWORD: "REPLACE_ME"
  POSTGRES_DB: "bot"
